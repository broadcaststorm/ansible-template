all:
  vars:
    # Define global variables here
  children:
    group1:
      hosts:
        host1:
          ansible_host: IP
          ansible_port: port
      vars:
        var=val
    # Repeat groups here

